sudo: false
language: node_js
node_js:
  - stable
cache: npm
branches:
  only:
    - sources # build master branch only
script:
  - hexo generate # generate static files
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GH_TOKEN
  keep-history: true
  on: main
  local-dir: public
